# 架构设计说明书 · 个人工具箱 — 记账模块 v1.0

---

> **阅读对象**：CodeBuddy（AI 开发者）
> **文档性质**：可执行架构规格 — CodeBuddy 必须严格遵循本文档定义的架构原则、数据契约、模块边界和实现顺序。
> **禁止事项**：不得修改本文档定义的模块边界和接口契约。如需调整，须先向用户确认。

---

## 第一章 · 产品概述

### 1.1 产品定位

个人使用的 iOS 工具集应用，当前仅开发**记账模块**。后续会扩展笔记等独立模块。无登录体系，打开即用。

### 1.2 用户画像

- 非程序员个人用户
- 使用 iOS 设备
- 需要管理个人财务记账
- 有自建服务器（云端功能暂不实现，仅预留接口）

### 1.3 核心需求列表

| ID | 需求 | 优先级 | 状态 |
|----|------|--------|------|
| R-001 | 导入微信/支付宝/自定义账单 CSV/Excel 文件 | P0 | 确定 |
| R-002 | 导入时用户选择文件来源类型 | P0 | 确定 |
| R-003 | 按字段映射为标准格式 | P0 | 确定 |
| R-004 | 导入后可按关键词自动分类，未匹配的弹窗手动分类 | P0 | 确定 |
| R-005 | 用户可自定义分类体系（大类+小类，不可交叉） | P0 | 确定 |
| R-006 | 记录收支，每笔含金额、分类、备注标签 | P0 | 确定 |
| R-007 | 区分收入与支出 | P0 | 确定 |
| R-008 | 混合方案查看：月总览 + 分类排行（大类/小类切换）+ 近期明细 | P0 | 确定 |
| R-009 | 可选去重（金额+时间+商户名三要素匹配） | P0 | 确定 |
| R-010 | 导出 CSV | P1 | 确定 |
| R-011 | 云端手动上传下载 | P2 | 预留，暂不实现 |
| R-012 | 每个功能模块可独立被 AI 迭代替换 | P0 | 架构约束 |

---

## 第二章 · 技术架构决策

### 2.1 技术选型

| 层面 | 选型 | 理由 |
|------|------|------|
| UI 框架 | SwiftUI（iOS 17+） | Apple 现代声明式 UI，与 ViewModel 天然配合 |
| 本地持久化 | SwiftData | Apple 原生方案，代码量少，与 SwiftUI 深度集成 |
| 架构模式 | MVVM + Clean Architecture | 分层清晰，测试友好 |
| 模块组织 | Micro-Function Architecture | 每个功能独立模块，可单独替换 |
| 最小部署目标 | iOS 17.0 | SwiftData 最低要求 |

### 2.2 架构分层（严格执行）

```
┌─────────────────────────────────────────┐
│ UI Layer（SwiftUI Views）               │
│ 只负责排版和响应事件，不碰业务逻辑        │
├─────────────────────────────────────────┤
│ ViewModel Layer（ObservableObject）      │
│ 持有 @Published 状态，调用 Use Case     │
├─────────────────────────────────────────┤
│ Domain Layer（Use Case / 纯 Swift）      │
│ 业务逻辑、数据校验、计算                 │
│ 不 import SwiftUI，不 import UIKit      │
├─────────────────────────────────────────┤
│ Data Layer（Repository + Parser）        │
│ 本地数据库、文件解析、导出生成           │
└─────────────────────────────────────────┘
```

**依赖规则**（CodeBuddy 必须遵守）：
- UI → 可访问 ViewModel ✅
- ViewModel → 可访问 Domain 层的 Use Case ✅
- Domain → 仅依赖 Repository 协议（interface），不依赖具体实现 ✅
- Data → 实现 Repository 协议 ✅
- **Domain 层禁止 import SwiftUI / UIKit / SwiftData** ❌
- **ViewModel 层禁止直接调用 Data 层** ❌
- **UI 层禁止直接操作 Data Layer 数据** ❌

### 2.3 微功能架构（Micro-Function Architecture）

整个 App 由独立的功能模块组成，每个模块包含自己的 Data + Domain + ViewModel + UI：

```
App Shell（模块注册 + 导航容器）
├── Import Module（导入功能）
│   ├── ImportParser（Data）
│   ├── ImportOrchestrator（Domain）
│   ├── ImportViewModel
│   └── ImportView
├── Category Module（分类管理）
│   ├── CategoryRepository（Data）
│   ├── CategoryUseCase（Domain）
│   ├── CategoryManageViewModel
│   └── CategoryManageView
├── Bill Module（账单管理）
│   ├── BillRepository（Data）
│   ├── BillUseCase（Domain）
│   ├── BillViewModel / HomeViewModel
│   └── BillView / HomeView
├── Export Module（导出）
│   ├── ExportService（Data）
│   ├── ExportUseCase（Domain）
│   ├── ExportViewModel
│   └── ExportView
└── Cloud Module（预留）
```

**关键约束**：模块之间禁止直接依赖。模块间通信通过 App Shell 协调，或通过共享的 Repository 协议。

---

## 第三章 · Data Layer（数据层）— 精确数据契约

### 3.1 SwiftData 模型定义

#### 实体 1：BillRecord（账单记录）

```swift
@Model
class BillRecord {
    // 主键
    var id: UUID
    
    // 交易时间 — 导入时的原始时间，或用户手动输入
    var transactionTime: Date
    
    // 收/支类型
    var type: TransactionType  // .income 或 .expense
    
    // 金额 — 精确到两位小数
    var amount: Decimal
    
    // 分类关联 — 可为 nil（未分类）
    var mainCategoryID: UUID?
    var subCategoryID: UUID?
    
    // 商户名 — 导入时从文件读取，只读展示，不可编辑
    var merchantName: String
    
    // 备注标签 — 用户自由填写，不限内容
    var tag: String
    
    // 记录创建时间 — 自动填充
    var createdAt: Date
    
    // 导入批次 ID — 用于去重检查
    var importBatchID: UUID?
}
```

**字段约束**：
- `id`：自动生成，不允许手动指定
- `amount`：使用 Decimal（禁止使用 Float/Double，避免精度丢失）
- `mainCategoryID / subCategoryID`：可为 nil，表示"未分类"
- `merchantName`：导入时从文件解析，用户不可编辑
- `tag`：默认空字符串
- `createdAt`：创建时自动设为当前时间

**枚举 TransactionType**：
```
enum TransactionType: String, Codable {
    case income   // 收入
    case expense  // 支出
}
```

#### 实体 2：MainCategory（大类）

```swift
@Model
class MainCategory {
    var id: UUID
    var name: String       // 如"饮食"
    var sortOrder: Int     // 排序序号，用户拖拽排序时更新
    var iconName: String   // SF Symbol 名称，用于 UI 展示
}
```

#### 实体 3：SubCategory（小类）

```swift
@Model
class SubCategory {
    var id: UUID
    var name: String           // 如"外卖"
    var mainCategoryID: UUID   // 所属大类 ID
    var sortOrder: Int         // 在大类内部的排序
}
```

**约束**：一个小类只能属于一个大类，不可交叉。删除大类时，其下所有小类也同步删除。

#### 实体 4：KeywordMap（关键词映射）

```swift
@Model
class KeywordMap {
    var id: UUID
    var keyword: String            // 匹配关键词，如"美团外卖"
    var subCategoryID: UUID        // 关联的小类 ID
    var priority: Int              // 匹配优先级，值越大优先匹配
}
```

**匹配规则**：按 priority 降序匹配，优先匹配**最长关键词**。例如同时存在"美团"和"美团外卖"两条映射，遇到商户名"美团外卖"时，优先匹配"美团外卖"。

### 3.2 Repository 协议（接口定义）

每个 Repository 是一个协议（Protocol），Data 层提供至少一个实现。Domain 层只依赖协议，不依赖实现。

```swift
// 📁 Data/Repositories/BillRepository.swift
protocol BillRepository {
    func fetchAll() async throws -> [BillRecord]
    func fetchByID(_ id: UUID) async throws -> BillRecord?
    func fetchByMonth(year: Int, month: Int) async throws -> [BillRecord]
    func fetchByDateRange(start: Date, end: Date) async throws -> [BillRecord]
    func fetchByCategory(mainCategoryID: UUID) async throws -> [BillRecord]
    func fetchByCategory(subCategoryID: UUID) async throws -> [BillRecord]
    
    func add(_ record: BillRecord) async throws
    func addBatch(_ records: [BillRecord]) async throws
    func update(_ record: BillRecord) async throws
    func delete(_ record: BillRecord) async throws
    func deleteBatch(_ records: [BillRecord]) async throws
}

// 📁 Data/Repositories/CategoryRepository.swift
protocol CategoryRepository {
    func fetchMainCategories() async throws -> [MainCategory]
    func fetchSubCategories(for mainCategoryID: UUID) async throws -> [SubCategory]
    func fetchAllSubCategories() async throws -> [SubCategory]
    
    func addMainCategory(_ category: MainCategory) async throws
    func updateMainCategory(_ category: MainCategory) async throws
    func deleteMainCategory(_ category: MainCategory) async throws
    
    func addSubCategory(_ category: SubCategory) async throws
    func updateSubCategory(_ category: SubCategory) async throws
    func deleteSubCategory(_ category: SubCategory) async throws
}

// 📁 Data/Repositories/KeywordMapRepository.swift
protocol KeywordMapRepository {
    func fetchAll() async throws -> [KeywordMap]
    func match(merchantName: String) async throws -> (mainCategoryID: UUID, subCategoryID: UUID)?
    func add(_ map: KeywordMap) async throws
    func update(_ map: KeywordMap) async throws
    func delete(_ map: KeywordMap) async throws
}
```

### 3.3 导入引擎（Parser 层）

```swift
// 📁 Data/ImportEngine/FileFormat.swift
enum FileFormat: String, CaseIterable {
    case wechat    // 微信账单
    case alipay    // 支付宝账单
    case custom    // 自定义模板
}

// 📁 Data/ImportEngine/BillParser.swift
protocol BillParser {
    func parse(fileURL: URL, format: FileFormat) async throws -> [RawImportRecord]
}

// 📁 Data/ImportEngine/RawImportRecord.swift
// 解析后的中间数据结构，未映射为标准 BillRecord
struct RawImportRecord {
    var transactionTime: String
    var typeRaw: String       // "收入"/"支出" 或 "+"/"-"
    var amountRaw: String
    var merchantName: String
    var productDescription: String
}
```

**字段映射规则（CodeBuddy 必须精确实现）**：

| 标准字段 | 微信账单读取列 | 支付宝账单读取列 |
|---------|---------------|-----------------|
| transactionTime | "交易时间" | "交易时间" |
| typeRaw | "收/支"（值：收入/支出） | "收/支出"（值：收入/支出） |
| amountRaw | "金额(元)" | "金额" |
| merchantName | "交易对方" | "交易对方" |
| productDescription | "商品" | "商品说明" |

### 3.4 导出服务

```swift
// 📁 Data/ExportService/ExportService.swift
protocol ExportService {
    /// 导出为 CSV 格式文件，返回文件本地 URL
    func exportToCSV(records: [BillRecord]) async throws -> URL
}
```

**CSV 生成规范**：
- 编码：UTF-8 with BOM（确保 Excel 正确打开中文）
- 分隔符：逗号 `,`
- 表头行：交易时间, 类型, 金额, 大类, 小类, 商户名, 备注标签
- 金额格式：保留两位小数（如 12.00）
- 文本中包含逗号或换行符时，用双引号包裹

### 3.5 云端服务（预留）

```swift
// 📁 Data/Cloud/CloudService.swift
// 注意：此协议仅作定义，**当前阶段不实现任何方法**
// 方法体保持空实现或 throw NotImplementedError

protocol CloudService {
    func upload(records: [BillRecord]) async throws
    func download() async throws -> [BillRecord]
    func lastSyncTime() async throws -> Date?
}
```

---

## 第四章 · Domain Layer（业务逻辑层）— Use Case 定义

每个 Use Case 是一个独立的结构体/类，可以单独被替换。

### 4.1 导入编排（ImportOrchestrator）

```swift
// 📁 Domain/Import/ImportOrchestrator.swift
class ImportOrchestrator {
    private let billParser: BillParser
    private let billRepository: BillRepository
    private let categoryRepo: CategoryRepository
    private let keywordRepo: KeywordMapRepository
    
    /// 完整导入流程
    /// 1. 解析文件 → 2. 映射为标准记录 → 3. 去重 → 4. 自动分类 → 5. 返回结果
    func execute(fileURL: URL, format: FileFormat, deduplicate: Bool) async throws -> ImportResult
}

struct ImportResult {
    let totalCount: Int
    let records: [BillRecord]          // 所有解析后的记录
    let matched: [BillRecord]          // 已自动匹配分类的
    let unmatched: [BillRecord]        // 未匹配分类的（需要弹窗）  
    let duplicates: [BillRecord]       // 被去重标记的
}
```

**去重逻辑**：当 `deduplicate == true` 时，对新导入的每条记录，检查数据库现有记录中是否存在 `transactionTime + amount + merchantName` 完全一致的记录。如有，标记为重复。

### 4.2 自动分类（AutoCategorizeUseCase）

```swift
// 📁 Domain/Category/AutoCategorizeUseCase.swift
class AutoCategorizeUseCase {
    private let keywordRepo: KeywordMapRepository
    
    /// 对单条记录尝试自动分类
    /// 返回匹配到的 (mainCategoryID, subCategoryID)，匹配失败返回 nil
    func categorize(record: BillRecord) async throws -> (UUID, UUID)?
    
    /// 批量处理：分出已匹配和未匹配
    func batchCategorize(records: [BillRecord]) async throws -> (matched: [(BillRecord, UUID, UUID)], unmatched: [BillRecord])
}
```

**匹配规则（严格执行）**：
1. 提取 `merchantName` 作为匹配源
2. 遍历所有 KeywordMap，按 priority 降序
3. 对每个 keyword，检查 merchantName 是否包含 keyword
4. 找到第一个匹配返回对应的 subCategoryID
5. 无匹配时返回 nil

### 4.3 月度统计（MonthlySummaryUseCase）

```swift
// 📁 Domain/Statistics/MonthlySummaryUseCase.swift
class MonthlySummaryUseCase {
    private let billRepository: BillRepository
    
    func calculate(year: Int, month: Int) async throws -> MonthlySummary
    func categoryRanking(records: [BillRecord], level: RankingLevel) async throws -> [CategoryRankingItem]
}

struct MonthlySummary {
    let totalIncome: Decimal
    let totalExpense: Decimal
    let balance: Decimal           // 结余 = 收入 - 支出
    let categoryBreakdowns: [CategoryBreakdown]
    let dailyAverage: Decimal      // 日均支出
}

struct CategoryBreakdown {
    let categoryName: String
    let totalAmount: Decimal
    let percentage: Double         // 占总支出的百分比
}

struct CategoryRankingItem {
    let categoryName: String
    let totalAmount: Decimal
    let percentage: Double
    let recordCount: Int
}

enum RankingLevel {
    case main   // 大类排行
    case sub    // 小类排行
}
```

### 4.4 分类管理（ManageCategoryUseCase）

```swift
// 📁 Domain/Category/ManageCategoryUseCase.swift
class ManageCategoryUseCase {
    private let categoryRepo: CategoryRepository
    
    // 大类 CRUD
    func createMainCategory(name: String) async throws -> MainCategory
    func renameMainCategory(id: UUID, newName: String) async throws
    func deleteMainCategory(id: UUID) async throws    // 同步删除其下所有小类
    func reorderMainCategories(orderedIDs: [UUID]) async throws
    
    // 小类 CRUD
    func createSubCategory(name: String, mainCategoryID: UUID) async throws -> SubCategory
    func renameSubCategory(id: UUID, newName: String) async throws
    func deleteSubCategory(id: UUID) async throws
    func moveSubCategory(id: UUID, toMainCategoryID: UUID) async throws
    func reorderSubCategories(mainCategoryID: UUID, orderedIDs: [UUID]) async throws
}
```

### 4.5 导出（ExportUseCase）

```swift
// 📁 Domain/Export/ExportUseCase.swift
class ExportUseCase {
    private let exportService: ExportService
    private let billRepository: BillRepository
    
    func exportAll() async throws -> URL
    func exportMonth(year: Int, month: Int) async throws -> URL
}
```

---

## 第五章 · ViewModel Layer（视图模型层）— 状态契约

### 5.1 HomeViewModel（首页总览）

```swift
// 📁 ViewModels/HomeViewModel.swift
class HomeViewModel: ObservableObject {
    // 状态
    @Published var selectedMonth: Date          // 当前查看的月份
    @Published var summary: MonthlySummary?     // 月度汇总数据
    @Published var rankingLevel: RankingLevel   // 排行级别：main / sub
    @Published var rankingItems: [CategoryRankingItem]
    @Published var recentRecords: [BillRecord]  // 最近7天明细
    @Published var isLoading: Bool
    @Published var errorMessage: String?
    
    // 生命周期
    func onAppear()                             // 页面加载时调用
    
    // 操作
    func switchToMonth(_ date: Date)            // 切换月份
    func toggleRankingLevel()                   // 切换 大类排行 / 小类排行
    func navigateToAddRecord()                  // 跳转记一笔
    func navigateToImport()                     // 跳转导入页面
    func navigateToCategory(_ id: UUID)          // 跳转分类详细
}
```

### 5.2 ImportViewModel（导入流程）

```swift
// 📁 ViewModels/ImportViewModel.swift
class ImportViewModel: ObservableObject {
    // 状态机 — 导入流程的各个阶段
    enum ImportStage {
        case selectSource       // 选择来源（微信/支付宝/自定义）
        case selectFile         // 选择文件
        case parsing            // 正在解析
        case mapping            // 展示映射结果 + 去重选项
        case autoCategorizing   // 自动分类中
        case manualCategorize   // 手动分类弹窗（需用户逐条确认）
        case completed          // 导入完成
        case failed(String)     // 导入失败
    }
    
    @Published var stage: ImportStage = .selectSource
    @Published var selectedFormat: FileFormat?
    @Published var parsedRecords: [BillRecord] = []
    @Published var matchedRecords: [(BillRecord, MainCategory, SubCategory)] = []
    @Published var unmatchedRecords: [BillRecord] = []
    @Published var duplicateRecords: [BillRecord] = []
    @Published var shouldDeduplicate: Bool = true   // 默认开启去重
    @Published var currentUnmatchedIndex: Int = 0   // 当前手动分类到第几条
    @Published var importProgress: Double = 0       // 进度 0~1
    
    func selectSource(_ format: FileFormat)
    func selectFile(url: URL)
    func confirmDeduplicate(_ enable: Bool)
    func confirmCategory(for record: BillRecord, mainCategoryID: UUID, subCategoryID: UUID)
    func skipRecord()                  // 跳过当前未匹配条目
    func finishImport()
    func cancelImport()
}
```

### 5.3 AddRecordViewModel（记一笔）

```swift
// 📁 ViewModels/AddRecordViewModel.swift
class AddRecordViewModel: ObservableObject {
    @Published var amount: String = ""
    @Published var type: TransactionType = .expense
    @Published var selectedMainCategory: MainCategory?
    @Published var selectedSubCategory: SubCategory?
    @Published var availableSubCategories: [SubCategory] = []
    @Published var tag: String = ""
    @Published var transactionTime: Date = Date()
    
    @Published var isSaving: Bool = false
    @Published var saveError: String?
    
    var isFormValid: Bool {
        !amount.isEmpty && 
        (Decimal(string: amount) ?? 0) > 0 &&
        selectedSubCategory != nil
    }
    
    func selectMainCategory(_ category: MainCategory)  // 同时更新 availableSubCategories
    func selectSubCategory(_ category: SubCategory)
    func save() async throws
}
```

### 5.4 CategoryManageViewModel（分类管理）

```swift
// 📁 ViewModels/CategoryManageViewModel.swift
class CategoryManageViewModel: ObservableObject {
    @Published var mainCategories: [MainCategory] = []
    @Published var selectedMainCategory: MainCategory?
    @Published var subCategories: [SubCategory] = []
    
    // 编辑状态
    @Published var isEditing: Bool = false
    @Published var editingItem: EditingItem?
    
    enum EditingItem {
        case addMain
        case renameMain(MainCategory)
        case addSub(MainCategory)
        case renameSub(SubCategory)
        case editKeyword(SubCategory)
    }
    
    func loadCategories()
    func addMainCategory(name: String)
    func deleteMainCategory(_ category: MainCategory)
    func addSubCategory(name: String, to main: MainCategory)
    func deleteSubCategory(_ category: SubCategory)
    func showKeywordEditor(for subCategory: SubCategory)
}
```

---

## 第六章 · UI Layer（视图层）— 页面定义

### 6.1 主页面结构

App 使用**单层导航**结构，底部为自定义 Tab Bar（不是系统 TabView）：

| Tab | 页面 | ViewModel |
|-----|------|-----------|
| 首页 | 总览仪表盘 | HomeViewModel |
| 账单 | 全部账单列表 | BillListViewModel |
| + | 记一笔（弹出模态） | AddRecordViewModel |

> **注意**：二级页面（导入、分类管理、分类详情）通过 NavigationStack 从首页 push 进入。

### 6.2 页面路由

```
App.start()
    ↓
HomeView（首页）
├── NavigationLink → ImportView（导入流程）
├── NavigationLink → CategoryManageView（分类管理）
├── NavigationLink → CategoryDetailView（某分类的账单列表）
├── sheet → AddRecordView（记一笔）
│
BillListView（账单列表）
├── NavigationLink → BillDetailView（单条账单详情/编辑）
│
SettingsView（设置）
├── NavigationLink → ExportView（导出）
├── NavigationLink → CloudSettingsView（云端设置 — 预留）
```

### 6.3 首页布局（精确描述）

```
┌─ Status Bar (62pt) ─────────────────────┐
│                                          │
│  我的工具箱                         [+]  │  ← Header
│                                          │
│  ┌─ 本月总览卡片 ──────────────────────┐ │
│  │  收入 ¥X,XXX    支出 ¥X,XXX          │ │
│  │  结余 ¥X,XXX                         │ │
│  │  [环形图：各分类支出占比]              │ │
│  └──────────────────────────────────────┘ │
│                                           │
│  ┌─ 分类排行 ─────────────────────────┐  │
│  │  大类排行 │ 小类排行    ← 可点击切换  │  │
│  │                                      │  │
│  │  饮食           ¥2,480    32%   ███  │  │
│  │  房             ¥1,800    24%   ██   │  │
│  │  车             ¥1,200    16%   ██   │  │
│  │  购物            ¥800    10%   █    │  │
│  └──────────────────────────────────────┘  │
│                                            │
│  ┌─ 近期明细 ──────────────────────────┐  │
│  │  今天                                 │  │
│  │  美团外卖    饮食·外卖        -¥32.00 │  │
│  │  中石化加油  车·加油         -¥300.00 │  │
│  │  昨天                                 │  │
│  │  星巴克       饮食·饮料       -¥38.00 │  │
│  │  京东         购物·网购       -¥159.00 │  │
│  └──────────────────────────────────────┘  │
│                                            │
│   [+ 记一笔]    [导入]    [导出]            │  ← 底部操作栏
│                                            │
├─ Tab Bar ─────────────────────────────┤
│    首页         账单         我的        │
└──────────────────────────────────────────┘
```

### 6.4 导入流程页面（精确描述）

```
Step 1: 选择来源
┌────────────────────────────────┐
│  选择账单来源                    │
│                                │
│  [📱 微信账单]  ← 点击选择      │
│  [💰 支付宝账单]                │
│  [📄 自定义模板]                │
│                                │
│  取消                           │
└────────────────────────────────┘

Step 2: 选择文件
→ 系统文件选择器（UIDocumentPickerViewController）
→ 支持 .csv 和 .xlsx

Step 3: 解析 & 去重
┌────────────────────────────────┐
│  解析完成：共 45 条记录          │
│                                │
│  ☑ 启用去重检查                  │
│  发现 3 条重复记录               │
│                                │
│  [查看重复]  [继续]              │
└────────────────────────────────┘

Step 4: 自动分类结果
┌────────────────────────────────┐
│  自动分类完成                    │
│                                │
│  ✅ 已匹配：38 条                │
│  ❓ 未匹配：7 条                 │
│                                │
│  [开始处理未匹配 →]              │
└────────────────────────────────┘

Step 5: 逐条手动分类（仅未匹配的）
┌────────────────────────────────┐
│  第 2/7 条                      │
│                                │
│  商户：滴滴出行                  │
│  金额：¥23.50                   │
│  时间：2026-07-25 14:30         │
│                                │
│  选择分类：                      │
│  大类：[交通 ▼]                  │
│  小类：[请选择 ▼]                │
│                                │
│  [跳过]          [确认]          │
└────────────────────────────────┘

Step 6: 完成
┌────────────────────────────────┐
│  ✅ 导入完成！                   │
│  成功导入 45 条记录              │
│                                │
│  [返回首页]                      │
└────────────────────────────────┘
```

---

## 第七章 · 初始数据（分类体系）

### 7.1 默认分类（App 首次启动时自动写入）

| 大类 | 包含小类 |
|------|---------|
| 饮食 | 饭卡, 聚餐, 外卖 |
| 购物 | 烟, 酒, 饮料, 网购 |
| 车 | 车贷, 充电, 加油 |
| 房 | 房贷, 家居用品, 物业费 |

### 7.2 初始关键词映射（首次启动时自动写入）

| 关键词 | 关联小类 |
|--------|---------|
| 美团外卖 | 外卖 |
| 饿了么 | 外卖 |
| 麦当劳 | 外卖 |
| 肯德基 | 外卖 |
| 海底捞 | 聚餐 |
| 食堂 | 饭卡 |
| 饭卡 | 饭卡 |
| 淘宝 | 网购 |
| 京东 | 网购 |
| 拼多多 | 网购 |
| 星巴克 | 饮料 |
| 瑞幸 | 饮料 |
| 喜茶 | 饮料 |
| 中石化 | 加油 |
| 中石油 | 加油 |
| 特来电 | 充电 |
| 国家电网 | 充电 |
| 宜家 | 家居用品 |
| 物业 | 物业费 |
| 房贷 | 房贷 |

---

## 第八章 · 实现顺序（CodeBuddy 执行顺序）

### Phase 1：基础设施（Data Layer 先做）

```
顺序 | 文件 | 说明
1    | Model 定义 | 所有 SwiftData @Model 实体
2    | Repository 协议 | BillRepository / CategoryRepository / KeywordMapRepository
3    | Repository 实现 | SwiftData 版本的具体实现
4    | BillParser 协议 | 导入引擎协议定义
5    | WeChatParser | 微信账单解析器
6    | AlipayParser | 支付宝账单解析器
7    | ExportService | CSV 导出实现
```

### Phase 2：业务逻辑（Domain Layer）

```
顺序 | 文件 | 说明
8    | MonthlySummaryUseCase | 月度统计（先用 mock 数据验证数据层通顺）
9    | ManageCategoryUseCase | 分类 CRUD
10   | AutoCategorizeUseCase | 关键词自动匹配
11   | ImportOrchestrator | 导入全流程编排
12   | ExportUseCase | 导出逻辑
```

### Phase 3：UI + ViewModel

```
顺序 | 页面 | 说明
13   | HomeView + HomeViewModel | 首页总览 + 排行 + 近期明细
14   | AddRecordView + VM | 记一笔表单
15   | ImportView + ImportViewModel | 全流程导入页面
16   | CategoryManageView + VM | 分类管理页面
17   | BillListView + VM | 账单列表
18   | ExportView | 导出页面
```

### Phase 4：测试 & 优化

```
19   | 边缘情况处理 | 空状态、加载中、错误提示
20   | 导入流程 UX 打磨 | 进度条、批量操作体验
21   | 分类切换排行动画 | 大类/小类切换的过渡效果
```

---

## 第九章 · 设计约束汇总（CodeBuddy 必须遵守）

1. **不使用第三方依赖**：所有功能使用 Apple 原生 SDK 实现。禁止引入 CocoaPods / SPM 第三方库。
2. **纯 SwiftUI**：不使用 UIKit 的 ViewController 进行页面跳转（UIDocumentPickerViewController 除外）。
3. **分类体系不可交叉**：一个小类只能属于一个大类，违反此规则的 CRUD 操作必须被拒绝。
4. **金额精度**：全部使用 Decimal，禁止 Float/Double。
5. **编码规范**：文件名、类名、变量名使用英文。UI 上显示的文本使用中文，存储在 Localizable 字符串或硬编码常量中。
6. **无登录**：App 不包含任何注册/登录/鉴权功能。
7. **XCConfig 配置**：云端服务器地址等运行时配置，放在单独的 Config.xcconfig 或 Config.swift 中，不硬编码在业务代码里。
8. **数据安全**：不使用 UserDefaults 存储账单数据。UserDefaults 只用于存储用户偏好设置（如默认去重开关状态）。
9. **删除保护**：删除大类时，必须同步删除其下所有小类和关联的关键词映射。删除操作前弹出确认对话框。
10. **首次启动**：App 首次启动时，自动写入 7.1 节的默认分类体系和 7.2 节的关键词映射。

---

## 附录 A · 文件目录结构

```
MyToolbox/
├── App/
│   ├── MyToolboxApp.swift          ← @main 入口
│   └── ModuleRegistry.swift        ← 模块注册表
│
├── UI/
│   ├── Home/
│   │   ├── HomeView.swift
│   │   ├── MonthlySummaryCard.swift
│   │   ├── CategoryRankingView.swift
│   │   └── RecentRecordsList.swift
│   ├── Import/
│   │   ├── ImportView.swift
│   │   ├── SourceSelectionView.swift
│   │   ├── ManualCategorizeView.swift
│   │   └── ImportCompleteView.swift
│   ├── Bill/
│   │   ├── AddRecordView.swift
│   │   ├── BillListView.swift
│   │   └── BillRowView.swift
│   ├── Category/
│   │   ├── CategoryManageView.swift
│   │   └── CategoryDetailView.swift
│   └── Export/
│       └── ExportView.swift
│
├── ViewModels/
│   ├── HomeViewModel.swift
│   ├── ImportViewModel.swift
│   ├── AddRecordViewModel.swift
│   ├── CategoryManageViewModel.swift
│   └── BillListViewModel.swift
│
├── Domain/
│   ├── Import/
│   │   └── ImportOrchestrator.swift
│   ├── Category/
│   │   ├── AutoCategorizeUseCase.swift
│   │   └── ManageCategoryUseCase.swift
│   ├── Statistics/
│   │   └── MonthlySummaryUseCase.swift
│   └── Export/
│       └── ExportUseCase.swift
│
├── Data/
│   ├── Models/
│   │   ├── BillRecord.swift
│   │   ├── MainCategory.swift
│   │   ├── SubCategory.swift
│   │   └── KeywordMap.swift
│   ├── Repositories/
│   │   ├── BillRepository.swift
│   │   ├── BillRepositoryImpl.swift
│   │   ├── CategoryRepository.swift
│   │   ├── CategoryRepositoryImpl.swift
│   │   ├── KeywordMapRepository.swift
│   │   └── KeywordMapRepositoryImpl.swift
│   ├── ImportEngine/
│   │   ├── BillParser.swift
│   │   ├── WeChatParser.swift
│   │   ├── AlipayParser.swift
│   │   └── RawImportRecord.swift
│   ├── ExportService/
│   │   └── ExportService.swift
│   └── Cloud/
│       └── CloudService.swift         ← 预留，暂不实现
│
└── Resources/
    ├── Assets.xcassets
    └── Config.swift                   ← 配置常量
```
